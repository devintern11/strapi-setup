'use strict';

/**
 * featureproduct controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::featureproduct.featureproduct');
